#include <stdio.h>

int main(void) {

float a,x,b,y;

printf(" Informe o valor de A=  ");
scanf("%f", &a);
printf(" Informe o valor de B= ");
scanf("%f", &b);
printf(" Informe o valor de X = ");
scanf("%f", &x);

 y = (a*x) + b;

printf(" valor da função: %.3f \n " ,y);
 return 0;
}
